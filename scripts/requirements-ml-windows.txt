# ML dependencies for Windows x64
# Fork of requirements-ml.txt:
# - Removes mlx-whisper (Apple Silicon only)
# - Uses CPU-only PyTorch (GPU transcription via CTranslate2's native CUDA)
# - Bundles pyannote for diarization (same as macOS)

# =============================================================================
# PyTorch ecosystem - CPU only
# GPU transcription uses CTranslate2's own CUDA bindings, not PyTorch CUDA.
# CUDA runtime DLLs (cublas, cudnn, cudart) are bundled as extraResources.
# =============================================================================
torch==2.8.0
torchaudio==2.8.0
# torchvision excluded from bundle to reduce size (~1GB)
# OCR feature installs it on-demand when user downloads OCR models

# =============================================================================
# HuggingFace ecosystem - version-critical for whisperx compatibility
# =============================================================================
huggingface_hub==0.36.1
transformers==4.48.0

# =============================================================================
# Transcription engines (no mlx-whisper on Windows)
# =============================================================================
whisperx==3.3.4

# =============================================================================
# Speaker diarization
# =============================================================================
pyannote.audio==3.3.2
pyannote-core==5.0.0
pyannote-database==5.1.3
pyannote-pipeline==3.0.1
pyannote-metrics==3.2.1

# =============================================================================
# Vision/OCR models
# =============================================================================
qwen-vl-utils==0.0.8
accelerate==1.3.0

# =============================================================================
# Semantic search embeddings
# =============================================================================
sentence-transformers==3.4.1

# =============================================================================
# Other ML dependencies with strict pins
# =============================================================================
numpy==2.0.2

# =============================================================================
# Voice assistant dependencies — on-demand install on Windows.
# livekit-rtc native DLL (~50MB) pushes the NSIS installer past its size limit.
# These are installed automatically when the user downloads a TTS model.
# =============================================================================
# livekit-api>=1.1.0
# livekit-agents>=1.5.0
# livekit-plugins-silero>=1.5.0
# livekit-protocol>=1.1.0
# livekit>=1.1.0
# kokoro-onnx>=0.4.0         # on-demand install (onnxruntime ~200MB)

# Live transcription VAD gate (Whisper hallucinates badly on silence —
# silero-vad lets us skip transcription on silent chunks). Tiny (~16 MB
# model, pure-Python wrapper); no native deps so it bundles cleanly on
# Windows.
silero-vad>=5.0
