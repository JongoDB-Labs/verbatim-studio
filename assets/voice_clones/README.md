# Voice clones bundled with Verbatim Studio

Place `max.wav` here (5-30s of clean speech). Optionally add `max.txt`
with the spoken transcript for higher-quality cloning.

This file is copied into the app bundle and migrated to user data on
first launch. Existing user voice clones in user data are never
overwritten.

